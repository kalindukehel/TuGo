import React, { useState, useEffect } from "react";
import { View, Text } from "react-native";

const TagActivity = (props) => {
  const { activity, navigation } = props;
  return (
    <View>
      <Text>TagActivity</Text>
    </View>
  );
};

export default TagActivity;
